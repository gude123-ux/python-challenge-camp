# 更新日志

## 1.7.0

学生反馈：「很多关卡用的都是之前关卡的原代码的改写，比如第十关第三题，但我懒得找第七天
那个东西了，你要在题目中把源代码贴过来」。

### 新增

- **★ 练习里直接贴出「这题要用到的前关代码」**
  - 新增 `src/core/refs.ts`：自动判断每道练习引用了哪一关的代码，判据三档：
    1. **显式引用**：题目写了 `day07` / `第 7 关` / `第 7 天` / `D7` → 直接取那一关的示例代码；
    2. **相对说法**：「上一关 / 昨天 / 上次」→ 取紧邻的前一关；
    3. **代码线索**：题目里的标识符（如 `scores`、`pass_rate`）出现在某关示例代码里
       （要求 2 个命中，或 1 个含下划线/较长的标识符命中）→ 认为那是底稿。
  - 关卡页的每道练习下面会**内联展开**这段源代码（含判定依据），并给两个按钮：
    「把这段代码追加到我的文件」/「打开第 N 关」
  - **新建关卡文件时**，这些代码也会以注释形式自动附在文件末尾（`# [前关代码] 第 N 关`），
    直接就能在同一个文件里改写
  - 实测覆盖：多数关卡的练习都命中了引用（例：第 10 关练习 1/3 → 第 7 关、
    第 15 关练习 3 → 第 14 关、第 45 关练习 1 → 第 42 关）
  - 这段引用代码**同时进精讲提示词**，所以「逐题提示」会贴着真实代码写

### 修复

- **引用识别里的死循环**（自己踩到并修掉）：判断显式引用时创建了带 `g` 标志的正则，
  循环里却调用了原来的非全局正则 —— 非全局正则的 `lastIndex` 永不前进，同一个匹配会
  无限返回。结果：**只要题目里出现 `dayNN` 就会卡死扩展宿主**（实测被系统 SIGTERM）。
  现在用带 `g` 的正则并加了零长度匹配兜底，且补了回归测试。
- `sourceCodeOf` 里原本用 `[\s\S]*?` 惰性量词正则去模板里抠示例代码，
  在 1~3KB 文本上会**灾难性回溯**；改成逐行扫描。

### 测试

- 新增 11 项：识别 day07/day14、带源代码、说明判定依据、无关题目不误报、
  「上一关」→ 前一关、注释块格式、精讲提示词带上引用代码、以及**不会卡死**的回归。
- 冒烟 219 项 + 加载 57 项全绿。


## 1.6.1

学生反馈：「所有关卡都要啊」—— 一关一关点太慢，所以补上批量生成。

### 新增

- **批量生成精讲**（命令：`批量生成精讲（所有未生成的关卡）`）
  - 一次把所有还没生成精讲的关卡跑完，**已生成的自动跳过**，所以可随时中断、下次续跑
  - 进度通知显示「第 N 关（x/y）」，可取消；失败会记下关卡号，再点一次就续上
  - 会先弹确认框，写清要跑多少关、预计耗时（按 45 秒/关估算）

- **命令行版批量脚本** `scripts/gen-tutorials.js`（适合首次铺满整本课程）
  ```bash
  node scripts/gen-tutorials.js              # 生成所有缺失的
  node scripts/gen-tutorials.js --from 8 --to 20
  node scripts/gen-tutorials.js --force      # 全部重新生成
  ```
  配置直接复用 VS Code 设置里的 `pythonCamp.*`（也可用 `--baseUrl/--apiKey/--model` 或环境变量）。
  实测：`glm-5.2` 每关约 36~60 秒，74 关约 50 分钟。

### 测试

- 加载测试新增 3 项：打包产物必须含「本关精讲 / 前置知识 / 批量生成精讲」。
- 冒烟 208 项 + 加载 57 项全绿。


## 1.6.0

这一版补的是**内容**：学生原话「知识点还是太少了，每一关我都不知道咋做」
「有的关需要用到之前关卡的内容，你帮我贴出来」「每一关提示要增加」。

### 新增

- **★ 本关精讲（AI 生成，讲透 + 分步 + 分级提示）**
  - 固定六节：`前置知识回顾` → `本关知识点精讲` → `本关要做什么（照着做）`
    → `逐题提示（卡住了再看）` → `常见错误` → `做完自查`
  - 知识点按 **是什么 → 为什么需要它 → 怎么写（最小例子）→ 新手最容易错在哪** 讲透，
    不再只罗列语法
  - 「本关要做什么」把练习拆成可执行步骤（打开哪个文件、先敲什么、看到什么算对）
  - 「逐题提示」**每题两级**：💡 思路提示（只讲思路与语法）+ 🔑 卡住了看这里
    （给 `___` 留空的代码骨架，**不给完整答案**）
  - 与「参考答案」分工明确：参考答案给完整代码（做完对照），精讲教你怎么做
  - **生成一次永久缓存**到 `python-camp/精讲/第NN关_精讲.md`，之后打开关卡直接读文件、不再调模型
  - 打开关卡时若还没有精讲，**后台自动生成**（失败静默不打扰）；也可手动点「本关精讲」重生成
  - 新命令：`本关精讲（讲透知识点 + 分步操作 + 逐题提示）`

- **★ 前置知识自动贴出来（本地算，不花 token、必然可用）**
  - 新增 `src/core/prereq.ts`：按三档判据找「这一关依赖的前面的关卡」——
    ① 同章且在前的关卡（越近权重越高）；② 知识点标签重合；③ 正文 4-gram 重合
    （中文不分词也能用：真正相关的两关会共享若干 4 字片段）
  - 关卡页新增「需要先会的前置知识」：直接贴出那些关的**知识点原文 + 一小段示例代码**，
    并说明「为什么它是前置」，还能一键跳到那一关
  - 这份前置知识同时会喂给精讲提示词，所以 AI 的「前置知识回顾」引用的是真实内容

- **知识点速览 + 精讲 + 练习的阅读顺序**：先知道要会什么（前置）→ 再看怎么做（精讲）→ 最后动手

### 说明

- 精讲用的是**本关及之前教过的语法**（提示词里写死），不会拿后面章节的东西要求零基础学生
- 单关精讲约 2000~3200 字，生成耗时 20~60 秒（取决于网关），生成后永久缓存

### 测试

- 新增 14 项：前置关卡判定（必须在前面、不重复、有原文、有理由、第 1 关无前置）、
  前置文本渲染、精讲提示词必须含「逐题提示 / 不给完整答案 / 前置知识回顾」且带上真实练习与讲义。
- 冒烟 208 项 + 加载 54 项全绿。


## 1.5.2

**根因确认**：实测用户配置的第三方网关（`opc.fiime.cn`）不稳定 —— 用真实批改提示词连打三次，
一次成功（17 秒），另外两次直接返回 **HTTP 503**。旧版本遇到 503 会立刻判「批改失败」，
而失败提示又卡在进度通知里（见 1.5.1），于是表现成「一直在批改中」。

### 新增

- **临时故障自动重试**（新设置 `pythonCamp.retryOnServerError`，默认开）
  - 遇到 **5xx（如 503）/ 网络错误 / 超时**时自动重试一次（退避 1.2 秒）
  - 重试过程会记进日志（`模型阶段=retry`），也能在进度里看到
  - 实测那种「三次里挂两次」的网关，开启后成功率明显提高
  - 不重试的情况（401/403/404/429/解析失败）保持原样，不浪费额度

### 测试

- 新增 3 项：假服务端首次 503 → 重试成功；关掉重试 → 只请求一次就报错；
  以及 1.5.1 的「响应体读不完要按时超时」回归。
- 冒烟 194 项 + 加载 54 项全绿。

## 1.5.1

针对「还是不行，一直在批改中」的第二次修复 —— 这次找到了真正让界面卡住的机制。

### 修复

- **★ 进度通知永远不消失（用户看到的「一直在批改中」）**
  - 根因：批改结束后要弹的提示（「进入第 N 关」/「AI 批改未完成，重试？」）是在
    `withProgress` 的回调**内部** `await` 的 —— 而进度通知包着这个回调。
    只要那个提示没被点掉，**进度通知就永远关不掉**，
    看起来就是「一直在批改」；而日志里连一行错误都没有（失败分支过去不写日志）。
  - 修法：所有需要用户点确认的交互**全部移出 `withProgress`**，
    进度通知在批改结束时一定关闭；失败分支也补上了日志。

- **★ 增加能自证的诊断（下次再卡能直接定位）**
  - 模型调用分阶段记日志：`request / headers / body / done`（带耗时、状态码、字节数）
  - 每 15 秒记一行心跳「仍在等待模型… 已 N 秒」—— 心跳还在说明事件循环正常、
    是请求本身没回来；心跳也停了说明扩展宿主被卡死。两种情况处理方式完全不同。
  - 每次批改都记「开始 / 调用模型 / 模型返回 / 结束」四行，带时间戳与触发来源

- **★ 看门狗：保证界面最终一定能动**
  - 业务层的总时限依赖「那个 promise 会走到收尾」；看门狗是**另一个独立定时器**，
    只要批改占着闸门超过 `总时限 + 60 秒`，就强制释放闸门 + 清掉进度卡片 +
    明确告诉用户「已强制中止，本次不记成绩，请查看日志」。

### 测试

- 新增回归：起一个「回了响应头就不再吐数据」的假服务端，
  断言客户端在超时内报错、且阶段回调能指出卡在「读完响应体」这一步。
- 冒烟 191 项 + 加载 54 项全绿。


## 1.5.0

这一版围绕「AI 批改」做四件事：不再一次批多关、不再卡死、过程看得见、改完给答案。

### 修复

- **★ AI 批改会卡死、一直显示「正在批改」不出结果**
  - 根因：`client.ts` 的超时只挂在 `await fetch()` 上，**一拿到响应头就 `clearTimeout`**，
    之后的 `res.text()` 完全没有超时保护。服务端（或中间代理）先回响应头、之后不再吐数据时，
    这里会永远挂着 —— 界面就永远停在「判分中」。
  - 修法：定时器覆盖到**读完响应体**为止，并把超时映射成明确的错误提示
    （带上已等待秒数 + 建议调大 `aiTimeoutSec`）。
  - 业务层再加一道**总时限**（`aiTimeoutSec × 2 + 60 秒`，纯本地检查 60 秒）：
    到点就中止本次批改、弹窗说明「本次不记成绩」，并解锁防重入闸门 —— 界面一定能收尾。
  - 批改过程现在**可取消**（通知栏有取消按钮），卡住时不用干等。

- **★ 「点一个 AI 批改，结果好几关都在批」**
  - 排查结论：全插件只有**一个**入口会一次批改多关 —— 侧边栏标题栏上那个
    「补交批改（写了代码还没提交的关卡）」按钮。它的图标容易被当成"提交"，点一下就会
    依次批改所有待提交关卡。
  - 修法：① 把该按钮从侧边栏标题栏**移除**（保留在「写了代码，还没提交」卡片里，
    以及命令面板）；② 批量确认弹窗写清「将依次批改 N 关、会调用 N 次模型」；
    ③ 批改**全局串行**：已有批改在跑时，再点只会提示「等它跑完」，不会并发；
    ④ 每次批改都往输出面板写一行 `[批改] 开始 · L05 · 触发来源=panel`，再出问题可直接查日志。
  - 顺带修掉两个会「批错关卡」的隐患：关卡页复用单例时的回调串档、
    批改结果/运行结果没按关卡作用域保存（在第 7 关可能看到第 5 关的分数）。

### 新增

- **★ 批改过程可视化**（不再是一句静止的「判分中」）
  - 关卡页出现「批改进度」卡片：读取文件 → 本地运行 → 收集终端记录 → 模型判分 →
    写入进度 → 生成参考答案，每一步带状态图标、整体进度条、以及「已等待 N 秒」
  - 通知栏同步显示当前步骤；进度只更新那一小块，不会重置页面滚动位置
- **★ 批改后自动给出参考答案与改进建议**
  - 触发条件：**没过关，或有题目没做出来**（全对且过关时不再多花一次模型调用）
  - 结果直接显示在关卡页底部（「参考答案与改进建议」），并落盘到
    `python-camp/参考答案/第NN关_参考答案.md`；下次直接读本地文件，不重复生成
  - 提示词里带上**本关讲义**，所以讲解与学生手上的教材一致
  - 手动点「AI 讲解本关」生成的内容也会同步显示到这一区块

### 工程

- 启动器改为**异步子进程**（原来用 `spawnSync`）：受限环境（沙箱 / 安全软件）会只拦同步启动，
  返回 EBUSY，表现为"编译失败""安装失败"这类误报。现在编译、装依赖、装 vsix 全部走异步，
  并且 `main()` 自己接住异常（双击失败时不会再一闪而过）。
- 加载测试在检测到同步子进程被禁时**明确跳过**启动器动态校验并说明原因，而不是报失败。
- 新增测试 12 项（总时限 / 计时回调 / 「批改后是否给答案」的判定 / 标题栏不再放批量补交按钮 /
  打包产物含进度与参考答案 UI）。
- 测试总数：冒烟 188 项 + 加载 54 项（本环境跳过启动器动态校验，正常环境为 75 项）。


## 1.4.0

两件事：一个 bug（点一次批改跑出两个），一个体验硬伤（知识点看不懂、不知道怎么下手）。

### 修复

- **★ 点一次「提交并批改」会跑出两个批改**
  - 根因：VS Code 可能对**同一个 webview view** 再次调用 `resolveWebviewView`
    （视图折叠再展开、面板被拖动等），而早期实现每次进来都注册一个新的
    `onDidReceiveMessage` —— 于是**一条消息被处理两次**，两个 AI 批改并发跑。
  - 实测证据：同一关的两次成绩只差 3~7 秒、评语还不一样（第 2 关 82/82、
    第 3 关 88/90、第 4 关 94/93）。
  - 两处修复：
    1. `SidebarProvider.resolveWebviewView` 做成**幂等**：同一个 view 只接一次监听，
       重新接之前先释放旧的；
    2. `submitLevel` / `runLevel` 加**防重入闸门**：同一关正在批改/运行时，
       再点只会提示「正在批改中，请稍等」，不会起第二个任务。
  - 副作用是省 token —— 每次提交从 2 次模型调用降到 1 次。

### 新增 / 改进

- **★ 每一关都有完整「本关讲解」**（学生只看关卡页就知道怎么动手）
  - 旧题库的「知识点」是 PDF 换行切碎的**半句**（「…把右边的结」/「果放进左边的盒子…」），
    学生读不懂，自然"不知道咋做"（用户实测反馈）
  - 重写提取算法：先把 PDF 硬换行**拼回完整段落**（中文换行处不加空格、中英之间补空格、
    去掉标点前的多余空格），再按小标题切块，并把每块配套的示例代码挂上去
  - 题库新增 `lesson` 字段：`[{heading, text, code}]`，每一关都有，平均约 1200 字符/关
  - 关卡页渲染成「本关讲解」（小标题 + 完整段落 + 配套代码），原来的知识点改为「知识点速览」
  - **讲义同时进提示词**：批改、参考答案、多种解法都会看到本关讲义，
    这样模型判分/写答案都会按"本关教过的方法"来，而不是拿后面的知识要求零基础学生
  - 文件模板顶部加了一行指引：「本关讲解见编辑器右侧『关卡详情』页的『本关讲解』」
  - 「知识点」也顺带修好了：现在每条都是完整句子（不再是半个词）

### 工程

- 新增测试 7 项（讲义覆盖度、段落完整性 >= 70% 以标点收尾、平均字符数、提示词是否带讲义）
- 加载测试新增 3 项：打包产物里必须真有「本关讲解 / 知识点速览 / 批改防重入」的代码
  （esbuild 默认把中文转义成 \uXXXX，测试里先解码再查）
- 测试总数：冒烟 180 项 + 加载 75 项


## 1.3.0

这一版解决一个**误解型**问题：学生以为「进度被清空了」，其实只是「今日任务跨天重派」+
「写了代码但没提交批改」。数据一直好好的，但界面没把这两件事说清楚。

### 新增

- **「写了代码但没提交」的检测与一键补交**
  - 扫描工作区里已存在的关卡文件：有自己写的代码（>= 2 行非注释）、但 progress 里
    `attempts` 为 0 的关卡，会被列出来
  - 侧边栏新增「写了代码，还没提交」卡片：逐关显示「已写 N 行代码」+「提交批改」按钮，
    以及「全部补交（N 关）」；关卡徽章新增 **待提交**（橙黄色），和「可挑战」区分开
  - 新增命令 `pythonCamp.submitPending`（侧边栏标题栏也有按钮）
  - 批量补交时用 quiet 模式跑：不弹需要点确认的对话框，避免卡在第一关上
  - 保存 .py 文件时自动重扫，写完立刻能看到提示
- 关卡详情页：如果这一关写了代码但没提交，页面顶部会明确提示「你写了 N 行代码，但还没提交过批改」

### 修复

- **消除「进度被清零」的误解**（用户实测反馈：「我昨天做了三关，怎么今天从第二关开始还把我进度弄没了」）
  - 侧边栏今日卡片下方新增说明：**今日任务每天 0 点重新派发**（这里是今天的清单，不是累计成绩），
    累计已过关 N/M 关**永久保留**
  - 派发今日任务的通知改成：「今日任务已派发：3 关（从第 2 关开始）。今日完成数每天归零，
    但累计已过关 N/M 关的成绩会永久保留。」；跨零点自动派发时同样说明
  - 关卡地图里，写了没提交的关卡显示「待提交」而不是「可挑战」

### 说明：为什么「从第二关开始」是正常的

每日任务按「学习前线」（第一关未过关的位置）派发。第 1 关过关后，前线推进到第 2 关，
所以第二天自然从第 2 关开始 —— 这与「进度丢失」无关。

### 工程

- 新增 `src/core/pending.ts`（纯函数 + 注入式 IO，便于测试）
- 新增测试 8 项：模板/有效代码计数、只挑「写了且未提交」、目录不存在、单文件读失败跳过
- 测试总数：冒烟 173 项 + 加载 72 项，连跑多轮全绿


## 1.2.0

这一版集中修四个用户实测反馈的问题，其中一个（过关线被别的插件覆盖）会直接让「过关」变成「没过关」。

### 修复

- **★ 双击快捷方式弹出两个不一样的 VS Code**
  - 根因：启动器默认走「开发宿主模式」（`--extensionDevelopmentPath`），**每次都会新开一个窗口**，
    与你自己开着的普通窗口并存
  - 现在默认走**安装模式**：先把插件装进 VS Code，再打开工作区 —— 只有一个窗口，进度也只有一份
  - 需要调试时才用 `--dev`（显式请求开发宿主）
- **★ 过了关但面板显示未过关 / 今日进度倒退**
  - 根因：本机曾同时存在**另一个同名插件**（`example.another-challenge-camp`），
    它和本插件都声明了 `pythonCamp.passScore`，默认值 60 / 80 互相覆盖；
    过关线被顶到 80 后，75 分的关卡被写回「未过关」
  - 三处修复：
    1. 过关线键改名为 `pythonCamp.passLine`（旧键只作为兼容读取，且只认你显式设置过的值）
    2. 过关状态**只升不降**（以最好成绩为准），重刷低分不会抹掉今日完成标记
    3. 每次激活自动跑一次**进度自愈**：`bestScore ≥ 过线分` 的关卡补回「已过关」并补回今日完成标记
- **★ AI 看不到终端内容，导致打分偏低**
  - 新增 `src/core/terminal.ts`：用 VS Code Shell Integration API 记录集成终端里
    **与 Python 相关**的命令与输出（`python -c "..."`、`python 第02关.py`、`pip list`…），
    随批改 / 报错分析 / 问答一起交给模型
  - 本地兜底评分也会用上终端证据：插件这次没跑通、但终端记录显示你自己跑通了，
    不再判「不可运行」（给分略低于直接跑通，并注明依据）
  - 提示词新增硬口径：**证据不足 ≠ 做错了** —— 看不到完成情况时只标「无法验证」并给自查方法，不许扣分
  - 隐私：非 Python 命令的输出**根本不进内存**；可用 `pythonCamp.terminalContext` 关闭
- **AI 批改没跑成时不再记成绩**
  - 实测拿到的失败原因是模型服务限流：`429 rate_limit_error / inference exceeds tpm/rpm limit`
  - 老版本会退回本地评分（封顶 85）并记为一次成绩，学生看到「未过关」却根本没被批改过
  - 现在：面板里显示本地参考分并注明「不计入过关判定」，弹窗给「重试 AI 批改 / 查看日志 / 先不批改」

### 新增

- **同名插件冲突检测**：启动时扫描是否装了另一个 `displayName` 相同的插件，
  弹窗说明危害并提供**「卸载它」按钮**（调 VS Code 自带的卸载命令，可取消）；启动器也会提前告警
- 设置项 `pythonCamp.terminalContext`（默认 `true`）
- 批改结果里显示「本次批改已参考你在集成终端里的 Python 命令与输出」
- 启动器新增 `--dev` 开关；`--dry-run` 保证零副作用（不再触发打包安装）
- 启动器安装失败时的可诊断性：不再只看退出码，而是**检查扩展目录里到底有没有这个版本**，
  失败时直接给出「Extensions: Install from VSIX…」的手动安装指引

### 工程

- 活动栏容器 ID 从 `pythonCamp` 改为 `pythonCampActivity`（独占，避免与同名插件的视图被合并到一个图标里）
- 命令 `pythonCamp.openSettings` → `pythonCamp.openConfig`（同上，避免命令 ID 撞车）
- 新增测试：终端证据 20 项（含隐私过滤、降级、提示词口径）、进度自愈 6 项、
  同名插件冲突 3 项、启动器默认模式回归 4 项 —— 冒烟 165 项 + 加载 72 项，连跑 3 轮全绿

## 1.1.0

三个新的 AI 功能。它们和「批改」的分工不同：批改要模型返回 JSON 并打分，
这三个产出的是**给人看的 Markdown**，所以走独立模块 `src/ai/tasks.ts`，不需要解析。

### 新增
- **报错自动分析**（`AI 分析报错原因`）
  - 会**先本地真跑一次**拿到真实 traceback，再把「代码 + traceback + 关卡知识点」交给模型
    —— 报错分析的价值就在这条 traceback 上，让模型凭空猜"可能哪里错了"没有意义
  - 产出结构固定：报错在说什么 → **错在哪一行** → 为什么会这样 → 怎么改（最小改动）
    → **下次怎么自己找到这类错误** → 改完怎么确认
  - 提示词里明确禁止"重写整份代码" —— 学生需要理解，不是抄
  - 代码能跑通时不做分析（没报错可分析），会直接告诉你
  - 结果显示在关卡详情页；本地运行失败时**自动触发**（设置 `autoDiagnoseOnError`，默认开），
    且**复用刚才那次运行结果**，不重复跑
- **多种解法**（`AI 列出多种解法`）
  - 至少 3 种思路**真的不同**的解法（不是换变量名），每种含：思路 / 可运行代码 / 优点 / 缺点 / 什么时候用它
  - 带一张解法总览表，末尾给**可操作的决策建议**（拒绝"各有优劣、视情况而定"这种废话）
  - 还专门列一节「看起来聪明、其实是坏习惯的写法」
  - 落盘为 `python-camp/参考答案/第NN关_多种解法.md` 并打开预览；已生成过则直接打开
- **随时提问**（`问 AI 助教`）
  - 独立的多轮问答面板：消息列表 + 输入框（Ctrl+Enter 发送）+ 快速提问快捷键
  - **自动带上当前关卡与你的代码作为上下文**，多轮对话里模型始终知道你在做哪一关
  - 支持导出对话到 `.pythoncamp/ai-chat.md`；历史只保留最近 10 轮，控制 token 开销
  - 侧边栏今日任务卡片和底部都有入口
- 新增设置项 `pythonCamp.autoDiagnoseOnError`（默认 `true`）

### 工程
- 新增 `src/ai/tasks.ts`：长文本任务统一走 `chat()`，自动共享超时、截断检测、错误分类
- 新增 `src/panels/chatPanel.ts`：问答面板
- 新增 `renderMarkdown()`（`src/panels/html.ts`）：自己实现的极简 Markdown 渲染，
  坚持**运行时零第三方依赖**；所有文本先过 `escapeHtml`，模型输出里的 HTML 不会被执行
- **长文本任务的下限保护**：`maxTokens` 取「设置值」与 8000 的较大者 ——
  设置里的默认值 4000 是给批改调的，讲解类任务要写 3 道题的思路 + 代码，很容易被截断

### 测试
- 新增 28 条用例（冒烟 111 → **139 项**），含：
  - 三个新提示词的结构断言（真实 traceback 是否进了提示词、是否要求指出行号等）
  - **Markdown 渲染的安全性**：模型输出里的 `<script>`、`<img>`、代码块里的 `</script>`
    必须被转义；未闭合的围栏也要能渲染出内容

## 1.0.3

### 修复
- **修复「模型忘了转义字符串内部双引号」导致的整份 JSON 报废**（第三种损坏模式）。
  真实案例：模型想引用一条命令，写成了

  ```
  "issues": ["…体现运行了`python -c "print(2026 - 2000, 10 / 4, 10 // 4)"`来观察…"]
  ```

  内层双引号没转义 → 字符串提前结束 → 后面全成非法 token → 整条批改退化成"本地评分"。

  这次不是"抢救"，是**真正修好**：新增 `repairUnescapedQuotes()` ——
  字符串里遇到的 `"`，若其后（跳过空白）不是 `,` `:` `]` `}` 也不是文本结尾，
  就判定为内容里的裸引号并补上 `\"` 转义。修复后整份 JSON 变得完全合法，
  `score / strengths / issues / suggestions / weakTags / exerciseChecks` **一个都不丢**。

  **关键安全性质：对合法 JSON 是恒等变换**（合法 JSON 里内容中的引号都已转义，
  所有未转义的 `"` 后必然紧跟分隔符，所以一个都不会被误改）。
  为此专门写了 10 条用例逐项钉死：字符串内的冒号/逗号/右方括号、
  空字符串、嵌套结构、已转义的引号、紧跟 `,` / `}` 的字符串结尾等。

- **提示词加固**（从源头减少复发）：在批改提示词里明确写出 JSON 转义硬要求，
  给出正反例，并引导模型**改用反引号 / 中文引号**引用命令，而不是英文双引号。
- **「抢救到残缺结果」也会自动重试**（之前只在彻底解析失败时重试）。
  上游偶发混流时，重试通常能拿到一份干净的**完整**结果，而不是残缺结果。
- 修正了抢救提示的文案：原文案把原因写死成"上游拼接"，但损坏可能是别的原因，改为通用描述。

### 测试
- 新增 21 条回归用例（冒烟 90 → **111 项**），含：
  - 用**真实遇到的未转义引号原文**做 fixture，断言完整解析 + issue 内容被原样重建
  - 10 条「恒等变换」用例，保证修复不会动到任何合法 JSON
  - 5 条提示词渲染用例，确认转义规则真的进了 SYSTEM（模板字符串里的反引号、反斜杠极易写错）

## 1.0.2

### 修复
- **应对「模型返回被上游拼接」这种损坏**。1.0.1 修的是解析器把好数据弄坏；
  这次是数据本身就坏：模型生成到一半**重新开始一份新草稿**，两份 JSON 被上游中转拼在一起 ——

  ```
  {"score":75,...,"strengths":["a","b","按格式要求完成了{ "score": 70, "runnable": true, ...
  ```

  （注意 score 75→70、correctness 60→50，是两份不同的生成）
  整体 parse 必然失败，这是**正确的**，不该硬修。
  但损坏点**之前**的字段其实完整可用 —— 于是新增**逐字段抢救**：
  按 `"key": value` 逐个解析（`readValue` 用括号栈 + 字符串状态机精确切分），
  遇到第一个不完整的就停。分数、可运行性、总评这些关键字段都排在前面，所以能救回来，
  不必整个退化成"本地评分"。
  抢救成功时结果会带 `salvaged` 标记，并在问题清单顶部提示「结构损坏、建议列表可能不完整」。
- **抢救也失败时自动重试一次**（`temperature=0`）。第三方中转偶发混流，重试通常能拿到干净结果。
  新增设置项 `pythonCamp.retryOnBadJson`（默认开启），代价是多等一次。

### 测试
- 新增 13 条回归用例，其中直接用**用户实测的损坏原文**做 fixture，断言：
  - 整体解析确实失败（该失败就得失败）
  - 逐字段抢救能拿到 score=75 / correctness=60（第一份草稿的值，不是第二份的 70/50）
  - 标记 `salvaged`、issues 带损坏提示
  - **保守性**：没有对象可抢救、只有一个残破 key、毫无结构的文本 → 一律返回 null，绝不凭空编分数
- 冒烟测试 77 → **90 项**，加载 **61 项**，全绿。

## 1.0.1

### 修复
- **修掉一个会让 AI 批改直接失效的解析 bug**：提示词要求模型在 `suggestions` 里给出 markdown 代码块，
  于是**合法 JSON 的字符串内部会包含 ` ``` `**。旧解析器用非贪婪正则 `/```(?:json)?\s*([\s\S]*?)```/`
  剥外层围栏，会被内层代码块骗到，从第一个 ` ``` ` 匹配到内层那个就停，只抠出几十字符碎片；
  更糟的是它把待解析文本覆盖成了这个碎片，连后面的花括号兜底也一起失效 ——
  于是一份完全正确的 JSON 被报成「模型返回的内容无法解析为 JSON」。
  改为三段式解析：**① 整体直接解析 → ② 逐字符括号配对（跳过字符串字面量）→ ③ 最后才考虑剥围栏（首尾配对）**。
  已用真实模型返回验证：旧逻辑只抠出 79 字符并失败，新逻辑完整解析出 score=45 与 3 条逐题判定。
  新增 6 条回归用例覆盖该场景。
- `client.ts` 增加健壮性：
  - 检测 `finish_reason === 'length'`，明确提示「输出被 max_tokens 截断」而不是笼统的解析失败
  - 推理模型只返回 `reasoning_content`、正文为空时，提示「token 被思考过程耗尽」
  - 支持数组型 `content`（`[{type:'text',text:'...'}]`）
- 批改失败时的错误信息现在带上**返回长度、结束原因和原始返回片段**，便于定位。

### 新增
- **命令「测试 AI 连接」**：逐项验证配置完整性、端点解析、网络与鉴权、以及**返回内容能否解析出 JSON**
  （最后一项是刻意加的 —— 只测连通性发现不了上面那个 bug）。结果输出到独立的「Python闯关训练营」输出通道，
  失败时按错误类型给出可操作的下一步建议。
- **命令「AI 讲解本关（含参考答案）」**：让 AI 逐题给出「考什么 → 思路 → 可运行参考代码」，
  外加完整可运行版本、易错点、自查方法；落盘为
  `<工作区>/python-camp/参考答案/第NN关_参考答案.md` 并打开预览。
  已生成过则直接打开，不重复调用；侧边栏「今日任务」卡片和关卡详情页都加了按钮。
  提示词里约束「只能用本关及之前教过的语法」，避免甩出超纲写法。
- 设置项 `pythonCamp.maxTokens`（默认 4000）与 `pythonCamp.aiTimeoutSec`（默认 120 秒）。

### 验证
- 两个新功能都用**真实模型**跑过端到端：
  - 测试连接链路：HTTP 4.2s、`finish_reason=stop`、JSON 解析出 `{"ok":true,"msg":"连接正常"}`
  - 参考答案：22 秒生成 2573 字符，5 个章节齐全、4 段可运行代码
- JSON 解析修复用**真实模型返回原文**做新旧逻辑对照：旧逻辑只抠出 79 字符并失败，新逻辑完整解析

### 测试
- 冒烟测试 71 → **77 项**，加载测试 **61 项**，全绿。

## 1.0.0

首个版本。

### 题库
- 题库由生成脚本产出，每关包含知识点、示例代码与练习题（本仓库内为通用占位示例）
- 每关包含：知识点简述、示例代码、编程练习题、验收标准、迁移视角
- 生成脚本 `scripts/build_levels.py` 可重复执行；`--with-aero` 可附加专项章节

### 核心功能
- **本地进度存储**：`<工作区>/.pythoncamp/progress.json`，原子写入 + 损坏自愈备份
- **每日任务派发**：跨天自动重置；每日任务即解锁通道（解决严格顺序解锁下每天只能打 1 关的问题）
- **闯关解锁**：过关后解锁下一关；支持手动解锁与全局跳关开关
- **代码复现**：新建关卡文件时，示例代码以注释形式写入模板
- **AI 自动批改**：本地真跑一次拿到真实 traceback，连同代码交给大模型判分（可运行性 / 正确性 / 代码质量 → 0~100）
- **本地兜底评分**：未配 API Key 或调用失败时退回启发式评分，不丢成绩
- **侧边栏进度面板**：今日任务 / 关卡地图 / 数据面板三页签
- **关卡详情页**：完整关卡内容 + 批改结果 + 历史成绩
- **学习时长统计**：VS Code 前台且有 Python 活动时计时
- **薄弱知识点**：批改扣分标签自动累计排序
- **错题重刷**：按错题重排今日任务
- **学习报告导出**：Markdown，含章节进度、成绩明细、薄弱点、30 天时长
- **中文 UI**，浅色/深色主题自动适配（全部走 VS Code 主题变量）

### 工程
- TypeScript strict 模式，esbuild 打包，运行时零第三方依赖（AI 客户端用 Node 内置 fetch）
- 11 个命令 + 2 组快捷键 + 编辑器右键菜单 + 状态栏指示器
- 12 个可配置项
- **一键启动器**（`launch.cmd` + `scripts/launch.js`）：双击桌面图标即可装依赖 → 编译 → 找 VS Code → 建工作区 → 启动
  - 优先正版 VS Code，不会被 PATH 里第一个 `code`（可能是 Qoder/Cursor 等分支）带偏
  - `.cmd` 纯 ASCII + CRLF，中文全部由 Node 输出（绕开中文系统 cmd 的 GBK 乱码）
  - 失败时写诊断日志到 `%TEMP%\python-camp-launch.log` 并 `pause`
  - 支持 `--install`（打包 vsix 正式安装）/ `--check`（环境诊断）/ `--dry-run`（决策验证）
  - 桌面启动器由 `scripts/make-desktop-launcher.py` 生成，带 CRLF/ASCII/BOM 自检，项目移动后可一键重生成
- **冒烟测试 71 项**：题库完整性、解锁规则、任务派发、AI 返回解析、本地评分、真实 Python 运行（含超时中断与中文编码）、持久化与自愈
- **加载测试 61 项**：打包产物加载、`activate()` 执行、package.json 与代码的命令/配置项一致性交叉校验、订阅释放、启动器与桌面启动器校验
